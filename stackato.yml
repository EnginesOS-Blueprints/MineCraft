name: minecraftserver-test
memory: 512
framework:
 type: php
requirements:
 running:
  ubuntu:
   - default-jdk  php5-curl wget 

hooks:
 pre-staging:
  - bash stackato-pre-setup.sh
 post-staging:
  - bash stackato-setup.sh
 pre-running:
  - sh completeinstall.sh
